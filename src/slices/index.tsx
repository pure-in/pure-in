import { SliceZoneComponents } from '@prismicio/react';
import { Slices } from 'core/prismic';
import Title from './Title';

export const components: SliceZoneComponents<Slices> = {
	title: Title,
};

